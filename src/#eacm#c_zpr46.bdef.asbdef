projection;
strict ( 2 );
use draft;

define behavior for /EACM/C_ZPR46 alias Zpr46
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}